import { ModuleStub } from "@/components/app/ModuleStub";

export default function Page() {
  return (
    <ModuleStub
      title="Hábitos"
      summary="Rejilla de 14 días por hábito con scroll horizontal, marcado del día y rachas."
    />
  );
}
