import { ModuleStub } from "@/components/app/ModuleStub";

export default function Page() {
  return (
    <ModuleStub
      title="Configuración"
      summary="Seis interruptores, unidades, idioma, modo oscuro, privacidad y exportación de datos."
    />
  );
}
